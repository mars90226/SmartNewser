extends layout

block content
  main
    aside
      img#logo(alt="icon", src="images/logo.jpg")
      #news_source
        | Source
        span BBC
        span CNN
      #news_date
        | Date
        span 1 day
        span 1 week
        span 1 month
        span 1 year
      #news_location
        | Location
        span Taiwan
        span Asian
        span Europe
        span America
    #content
      header.row
        ul#filter.col-md-offset-1.col-md-8
          li BBC
          li 1 year
          li Taiwan
        input#search.col-md-2(placeholder="Search")
      section.row
        .column.col-md-4
          include includes/article1
        .column.col-md-4
          include includes/article2
        .column.col-md-4
          include includes/article3
    

  script.
    $("[id^='news_']").children("span").click(function(){
      var span = $(this);
      if (span.hasClass("is-clicked")) {
        span.removeClass("is-clicked");
        $("#filter > li").each(function(){
          var li = $(this);
          if (li.text() === span.text()) {
            li.remove();
          }
        });
      } else {
        span.addClass("is-clicked");
        $("<li>" + span.text() + "</li>").appendTo("#filter");
      }
    });
